# Database
DATABASE_URL=postgresql://freecord:freecord@localhost:5432/freecord

# Redis
REDIS_URL=redis://localhost:6379

# JWT
JWT_SECRET=change-me-to-a-random-32-char-string
JWT_REFRESH_SECRET=change-me-to-another-random-32-char-string
INTERNAL_TOKEN=change-me-to-a-random-32-char-internal-token

# API
API_PORT=3000
API_HOST=0.0.0.0
CORS_ORIGIN=http://localhost:5173

# Gateway
GATEWAY_PORT=8080
GATEWAY_HOST=0.0.0.0

# Voice
VOICE_PORT=8081
VOICE_HOST=0.0.0.0
VOICE_LISTEN_IP=0.0.0.0
VOICE_ANNOUNCED_IP=127.0.0.1
VOICE_RTC_MIN_PORT=40000
VOICE_RTC_MAX_PORT=49999

# CDN
CDN_PORT=3001
CDN_HOST=0.0.0.0
CDN_MAX_FILE_SIZE=104857600

# MinIO (S3-compatible storage)
MINIO_ENDPOINT=localhost
MINIO_PORT=9000
MINIO_ACCESS_KEY=minioadmin
MINIO_SECRET_KEY=minioadmin
MINIO_BUCKET=freecord
MINIO_USE_SSL=false

# App URLs (used by the React client - MUST be set before running `pnpm build`)
# Replace localhost with your server's public IP/domain when deploying
VITE_API_URL=http://localhost:3000
VITE_GATEWAY_URL=ws://localhost:8080/gateway
VITE_CDN_URL=http://localhost:3001
VITE_VOICE_URL=http://localhost:8081

# GIF Picker (optional) - get a free key at tenor.com/developer/keyregistration
# VITE_TENOR_API_KEY=
